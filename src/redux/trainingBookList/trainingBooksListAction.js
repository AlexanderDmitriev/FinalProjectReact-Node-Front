import { createAction } from '@reduxjs/toolkit';

export const bookList = createAction('users/bookList');
export const startDate = createAction('users/startDate');
export const finishDate = createAction('users/finishDate');
